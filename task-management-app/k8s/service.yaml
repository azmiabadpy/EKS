apiVersion: v1
kind: Service
metadata:
  name: task-management-service
  namespace: task-management
spec:
  type: LoadBalancer

  selector:
    app: task-management-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
